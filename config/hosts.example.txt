# 批量 Agent 部署清单示例（在中心机上交给 deploy_fleet.sh，不要笔记本旁路 scp）
# 格式: IP[:port] [host_id] [ssh_port]
# 端口可选，默认 22（或 deploy_fleet.sh --ssh-port / --port）；不会扫描端口
# 远端目录默认 /opt/monitor-agent（--remote-dir）；Center 仍用 /opt/monitor
# 10.0.0.11 npu-01
# 10.0.0.12 npu-02 2222
# 10.0.0.13:2222 gpu-13
# 10.0.0.21 app-01
